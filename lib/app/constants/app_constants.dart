/// Application constants (UI/App level only)
class AppConstants {
  static const String appName = 'WalletUp';
  static const String appVersion = '1.0.0';
  // TODO: Read version with package_info_plus and expose via provider.
}
